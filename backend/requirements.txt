# ── FORGE backend dependencies ───────────────────────────────────────────────
# Web / transport
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
python-multipart==0.0.20

# Config
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1

# HTTP client for the tool-calling sidecar (DashScope OpenAI-compatible chat-completions)
httpx>=0.27

# Qwen Cloud — official DashScope SDK (idiomatic connector; the production bridge uses
# the direct realtime WebSocket protocol in app/realtime/session.py for fine-grained
# tool/image control — see app/realtime/connectors.py).
dashscope>=1.23.9

# Alibaba Cloud — OSS object storage (deployment proof)
oss2>=2.18.0

# Optional: AgentScope (Alibaba's agent framework) as an alternative realtime connector.
# Not on the critical path and version availability varies, so install separately if
# you want the AgentScope-idiomatic path:  pip install agentscope
# agentscope>=1.0

# Data handling
# (catalogs are parsed from CSV/JSON with the stdlib csv/json modules — no pandas
#  needed at runtime; keep the image footprint small)

# ── Test / dev only ──────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
jsonschema==4.23.0
